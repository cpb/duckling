brew "rust"
brew "gh"
brew "hk"

# No entry for zic (the tz compiler test/support/tz_fixtures.rb shells out to):
# macOS ships /usr/sbin/zic as a stock utility. Homebrew's tzdb formula would
# be redundant, and would shadow the compiler the host's own zoneinfo was
# built with.
