/.bundle/
/.yardoc
/_yardoc/
/coverage/
/doc/
/pkg/
/spec/reports/
/tmp/
.env.local
.worktree-session.json
pr_context.md
RESEARCH_CONTEXT.md
/target/
lib/duckling/duckling.bundle
lib/duckling/duckling.so

# Per-leg bundles (BUNDLE_LOCKFILE) — see the Gemfile and "The tz-database
# axis" in AGENTS.md. Never committed; the default Gemfile.lock is.
/Gemfile.*.lock
